<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Holo.Light">
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textColor">@color/default_text</item>
    </style>

    <style name="verticalContainers">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">fill_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:divider">@drawable/line_gradient_start</item>
        <item name="android:showDividers">middle</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="defaultTextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/default_text</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textSize">@dimen/default_text_size</item>
        <item name="android:singleLine">true</item>
    </style>

    <!-- Resource selection layout. -->
    <style name="ResourceView">
        <item name="android:layout_width">@dimen/control_icon_side</item>
        <item name="android:layout_height">@dimen/control_icon_side</item>
        <item name="android:clickable">true</item>
        <item name="android:gravity">top|right</item>
    </style>

    <style name="notificationView">
        <item name="android:onClick">onNotificationViewClick</item>
        <item name="android:clickable">true</item>
        <item name="android:layout_width">@dimen/notification_point_width</item>
        <item name="android:layout_height">@dimen/notification_point_height</item>
        <item name="android:text">@string/notification_number</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">right</item>
        <item name="android:background">@drawable/small_notification</item>
    </style>

    <!-- Water control page. -->
    <style name="smallTextView" parent="defaultTextView">
        <item name="android:textSize">@dimen/small_text_size</item>
    </style>

    <style name="valueTextView" parent="defaultTextView">
        <item name="android:textSize">@dimen/weathercast_item_temperature_text_size</item>
    </style>

    <!-- Space view-->
    <style name="horizontal_space">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:visibility">invisible</item>
    </style>
</resources>
